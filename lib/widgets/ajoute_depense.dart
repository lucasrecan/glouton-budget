import 'package:flutter/material.dart';

class AjouterDepense extends StatelessWidget {
  const AjouterDepense({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Ajouter Depense")),
      body: const Center(child: Text("Page Ajouter Depense")),
    );
  }
}